export const getJobs = (state) => state?.jobs;

export const getFilters = (state) => state?.filters;

export const getSearchValue = (state) => state?.searchInputValue;

export const getAppliedFilters = (state) => state?.appliedFilters;

export const getSorters = (state) => state?.sorters;
